name: Build and Update GitOps

on:
  push:
    branches:
      - dev
      - main

jobs:
  build-and-update:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout app repo
      uses: actions/checkout@v4

    - name: Set IMAGE TAG
      id: vars
      run: echo "SHA_TAG=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

    - name: Login to DockerHub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Build and Push Image
      uses: docker/build-push-action@v6
      with:
        context: ./myapp
        platforms: linux/amd64,linux/arm64
        push: true
        tags: ${{ secrets.DOCKERHUB_USERNAME }}/myapp:${{ steps.vars.outputs.SHA_TAG }}

    - name: Clone GitOps Repo
      run: |
        git clone https://x-access-token:${{ secrets.GIT_TOKEN }}@github.com/maorgigi123/helm-gitops-repo.git

    - name: Update Helm values file
      run: |
        cd helm-gitops-repo

        if [ "${{ github.ref_name }}" = "dev" ]; then
          VALUES_FILE="charts/myapp/values-dev.yaml"
        else
          VALUES_FILE="charts/myapp/values-prod.yaml"
        fi

        sed -i "s/tag:.*/tag: \"${{ steps.vars.outputs.SHA_TAG }}\"/" $VALUES_FILE

        git config user.name "github-actions"
        git config user.email "github-actions@github.com"

        git add $VALUES_FILE
        git commit -m "Update image tag to ${{ steps.vars.outputs.SHA_TAG }}"
        git push
